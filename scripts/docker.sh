#!/usr/bin/env bash
# Compatibility wrapper for the repo's docker helper (moved from project root)
set -euo pipefail

# This script was moved into scripts/ to keep all helpers in a single place.
echo "This is a repo helper. Run 'docker build' or use the Dockerfile in /docker'." 
